# Governance

Use only a validated canonical work-packet JSON object. Normal modes may read and write only task drafts; Green mode is subject to the separate edit/build-loop rule.

Bob never runs Bazaar mutation commands and never commits, merges, or tags. Bob does not access actual machines, control networks, mainline branches, or secrets.

Custom modes do not provide a command allowlist. Execute is therefore a governance-only restriction: it is available only in Green mode for the approved local VC6 workflow and only after soft-execute risk acceptance in the work packet and exception record.
